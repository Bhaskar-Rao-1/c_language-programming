#include<stdio.h>
//variable is the name of the memory location it's stores some data

int main()
{
    int age=10;//Variable
  //  int _age=29;
  //  int $age=30;
    printf("age value is %d \n",age);
    char star='B';//character should be in single quations
    printf("char is %c\n",star);
    float price=3.14;
    printf("float value is %f",price);

    return 0;
}